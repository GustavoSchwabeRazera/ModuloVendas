# Código anterior

Os arquivos nesta pasta pertencem à primeira versão do Módulo Vendas.
Eles foram preservados apenas como referência e não são executados pelo novo serviço.

O serviço novo inicia por `app.main:app` e usa o endpoint `POST /api/v1/prospeccoes`.
